{
  // Tareas automáticas para VS Code
  // Estas tareas se ejecutan automáticamente antes de iniciar la API
  "version": "2.0.0",
  "tasks": [
    {
      "label": "install-dependencies",
      "type": "shell",
      "command": "${workspaceFolder}/venv/Scripts/python.exe",
      "args": ["-m", "pip", "install", "-r", "requirements.txt"],
      "windows": {
        "command": "${workspaceFolder}/venv/Scripts/python.exe"
      },
      "linux": {
        "command": "${workspaceFolder}/venv/bin/python"
      },
      "osx": {
        "command": "${workspaceFolder}/venv/bin/python"
      },
      "group": "build",
      "presentation": {
        "echo": true,
        "reveal": "always",
        "focus": false,
        "panel": "shared"
      },
      "problemMatcher": []
    },
    {
      // 🔍 Tarea 3: Verificar configuración del entorno
      "label": "Verificar Entorno",
      "type": "shell",
      "windows": {
        "command": "python --version && pip --version && echo '✅ Python y pip están instalados correctamente'"
      },
      "linux": {
        "command": "python3 --version && pip3 --version && echo '✅ Python y pip están instalados correctamente'"
      },
      "osx": {
        "command": "python3 --version && pip3 --version && echo '✅ Python y pip están instalados correctamente'"
      },
      "presentation": {
        "echo": true,
        "reveal": "always",
        "focus": true,
        "panel": "shared",
        "showReuseMessage": false
      },
      "problemMatcher": []
      // ℹ️ EXPLICACIÓN:
      // Esta tarea verifica que Python y pip estén instalados correctamente.
      // Útil para diagnosticar problemas de instalación.
      //
      // Para ejecutarla manualmente:
      // 1. Ctrl+Shift+P
      // 2. "Tasks: Run Task"
      // 3. Seleccionar "Verificar Entorno"
    },
    {
      // 📦 Tarea 4: Mostrar dependencias instaladas
      "label": "Listar Dependencias Instaladas",
      "type": "shell",
      "windows": {
        "command": "pip list"
      },
      "linux": {
        "command": "pip3 list"
      },
      "osx": {
        "command": "pip3 list"
      },
      "presentation": {
        "echo": true,
        "reveal": "always",
        "focus": true,
        "panel": "shared",
        "showReuseMessage": false
      },
      "problemMatcher": []
      // ℹ️ EXPLICACIÓN:
      // Muestra todas las librerías de Python instaladas en el entorno.
      // Útil para verificar versiones o si falta alguna dependencia.
      //
      // Para ejecutarla manualmente:
      // 1. Ctrl+Shift+P
      // 2. "Tasks: Run Task"
      // 3. Seleccionar "Listar Dependencias Instaladas"
    }
  ]
}
