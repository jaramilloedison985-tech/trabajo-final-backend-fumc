{
  // Configuración para ejecutar y depurar la API con VS Code
  // Presiona F5 para iniciar la API en modo debug
  // 🎯 AUTOMÁTICO: Instala dependencias antes de ejecutar
  "version": "0.2.0",
  "configurations": [
    {
      // Configuración para ejecutar la API FastAPI
      "name": "🚀 FastAPI - Tienda Virtual",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["main:app", "--reload", "--host", "0.0.0.0", "--port", "8000"],
      "jinja": true,
      "justMyCode": false,
      "console": "integratedTerminal",
      "env": {
        // Las variables de entorno se leen del archivo .env
        // pero puedes sobreescribirlas aquí si es necesario
      },
      // ✨ TAREA PREVIA: Instala dependencias automáticamente antes de ejecutar
      "preLaunchTask": "install-dependencies",
      "presentation": {
        "hidden": false,
        "group": "API",
        "order": 1
      }
    },
    {
      // Configuración alternativa: ejecutar main.py directamente
      "name": "🐍 Python - main.py",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "console": "integratedTerminal",
      "justMyCode": false,
      // ✨ TAREA PREVIA: Instala dependencias automáticamente antes de ejecutar
      "preLaunchTask": "Instalar Dependencias",
      "presentation": {
        "hidden": false,
        "group": "API",
        "order": 2
      }
    }
  ]
}
